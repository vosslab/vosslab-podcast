A Bluesky post is a short social media update, similar to a tweet.
Write 1-2 plain text sentences about today's engineering work.
Target length: about {{char_limit}} characters.
Use concrete nouns and literal technical language: repo names, file names, and counts.
Rules:
- Return one plain-text line.
- Include at least one repository name and concrete counts.
- Use the context as the complete factual source.
When detail is limited, state 'No details in data' and continue.

Example of the desired specificity. Use Context JSON for the response facts:
'13 commits to bkchem today: theme system via YAML configs, toolbar separators, and hex grid rotation. Also 11 commits to vosslab-skills.'

Context JSON:
{{context_json}}
