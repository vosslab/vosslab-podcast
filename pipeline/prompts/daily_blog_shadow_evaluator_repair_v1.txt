# Structured shadow evaluation

Transform the earlier response into one JSON object containing integer scores from 1 through 5 for
`factual_grounding`, `changelog_use`, `thematic_structure`, `reader_interest`, and
`house_style_match`. Use `close`, `partial`, or `weak` for `verdict`. Include one concise `reason`.
The response consists exclusively of the JSON object.

## Earlier response

{response}
