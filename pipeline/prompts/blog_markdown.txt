You are writing a daily engineering blog update from GitHub activity data.
Write a human-readable Markdown post for MkDocs Material.
Target length: about {{word_limit}} words.
Required format:
- Use Markdown.
- One H1 title chosen by you.
- Write in human-readable paragraph form.
- Write in first person singular voice (use 'I' where natural).
- Keep the structure simple and readable.
- Use short prose paragraphs as the primary form.
- Ground every factual detail in the context data.
- Include repository names exactly as provided.
- Include concrete numbers from the data (commits/issues/PRs).
- Mention at least two repositories by name.
Tone: Write like a working engineer describing their day to a colleague.
Use short declarative sentences. Prefer concrete nouns: repo names,
file names, functions, and counts. Use literal technical language.
State 2-5 specific changes per repo. Each change must include one anchor:
a file path, function name, PR number, or commit subject.
Use the context as the complete factual source. When detail is limited, state
'No details in data' and continue. Keep the tone concrete, technical, and
matter-of-fact. Conclude with the clearest supported state of the day's work.

Context JSON:
{{context_json}}
Example of the desired specificity and rhythm. Use Context JSON for the response facts:
'# Toolbar polish and theme work in bkchem

Thirteen commits to bkchem. The big one was a YAML-based light/dark theme
system replacing the old hardcoded colors (commit: add theme yaml config).
Also rotated the hex grid from flat-top to pointy-top, which touched eight
files in the rendering module.'
