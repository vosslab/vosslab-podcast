# Remaining daily-blog work

This list records unfinished work only. The linked plans and operations guides remain authoritative.

## Complete the activation evidence

1. Run the sealed-fixture busy and quiet prompt experiment through deterministic author and referee
   role fakes. Record its complete samples, comparisons, and redacted artifacts. The capture command
   has no calibration input; see [prompt_experiment_status.md](active_plans/reports/prompt_experiment_status.md).
2. Run fixture-backed rubric calibration over the fixed historical corpus. Record exact-passage
   grounding, configured qualitative-consistency tolerance, target bands, and aggregate separation
   in the passing artifact. Aug. 22-23 are positive references, Aug. 24-25 voice failures, and Aug.
   26 is an evidence/discovery failure.
3. Create the deterministic, route-free attestation joining the sealed experiment capture and passing
   fixture-backed calibration artifact. This is the only step that consumes both artifacts.

## Review, activate, and audit v4

1. Have the configured independent reviewers work only from the sealed F4 artifacts. Each must read
   both complete selected posts, answer the central maker question, and cite exact passages for
   every required dimension. Choose an arm only after every required review passes both fixtures.
2. After a positive review, make the separately reviewed producer and publisher activation change
   described in [better_prompt_plan.md](active_plans/better_prompt_plan.md#milestone-16-activate-and-record).
3. Produce a complete blog page through the real producer, bundle, publisher import, strict MkDocs
   build, and verified-page path in disposable roots; then audit the activated boundary.
