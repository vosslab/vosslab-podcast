# Daily-blog verification backlog

The daily-blog rebuild is accepted. Stage 6 now derives its writer and editor
context, admission checks, and sealed publisher projection from one frozen
`PublicationSurface`; only the selected eligible `CompletePost` crosses the
publication boundary. The retired prompt-experiment, calibration, attestation,
and shadow-evaluation procedures have no remaining work items. The accepted
rebuild record is [daily_blog_rebuild.md](archive/daily_blog_rebuild.md).

## Optional host verification

- On each intended publication host, run the configured live Hermes route and
  record its provenance separately from controlled E2E evidence. Verify the
  resulting date-owned publication through its sealed bundle and rendered page.

This is optional corroboration, not an implementation task or a prerequisite
for the accepted no-egress validation path.
