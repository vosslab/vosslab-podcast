# Daily-blog fixup evidence

Status date: August 29, 2026

Status: complete

## Scope baseline

- Producer revision: `8cade7b31ed7f69723cfe20f6d3115c4858a9cdc`.
- Publisher revision: `d20896739bbc1d39f6b9daa7cc6b119cacc7375d`.
- The producer started clean. The publisher began with staged and unstaged work; F5 integration
  edits were applied without reverting or absorbing unrelated publisher changes.
- Active v4-maker policy digest:
  `3a4b7148579e509b6c32fa19b31d107dc4278eb5f721b2a01353a1a9a51264ee`.
- Tracked service digest:
  `57f8f8bb08a572d9c4f0c996da773a914263850542e2558eed9c26bcc4704932`.
- Tracked timer digest:
  `20e6aa37b3232bf5d58dc1ae767781af76044282d6c81319a5f51a67db7dd59b`.
- The installed service and timer digests differ from the tracked units. Installed-host inspection is
  optional corroboration; F6 uses the tracked units and disposable-root schedule proof.

## F0 through F3

The authoritative repository roster, sealed Aug. 23 quiet fixture, sealed Aug. 26 busy fixture,
prompt resources, candidate policies, bundle boundary, calibration contract, capture contract, and
deterministic attestation path are present. Production selects
`v4-three-examples-corpus-v2` through activation
`daily-blog-maker-activation-6b104be9c6907eeeffcf330f6b10173857b39c6b05baa46d4cf009a67daa7547`.

The literature-backed v4 prompt uses the project-owned Aug. 23 post as its full house-voice example.
It tests instruction-only, one-example, and three-example arms. The author receives a maker brief
and representative prose patterns rather than the referee's numerical rubric. The referee retains
the central complete-post question and the weighted criteria. Deterministic validators continue to
own evidence grounding, repository links, size bounds, and publication integrity.

Focused F1-F3 contract verification passed:

```text
146 passed in 2.56s
```

The latest focused route, editorial, capture, calibration, and attestation selection passed 92
offline tests. Pyflakes, source-size, function-typing, pytest-hygiene, ASCII, prompt-resource, and
Markdown-link checks passed 858 tests. The complete repository suite passed all 2,418 tests under
Python 3.12.13, and the retained E2E runner passed eight of eight workflows.

Route-free calibration preparation reproduced identity
`0df85dd7fdd48428353d0e6bde893acfaa21d4b23f66ffd267565a36c2ce6169` without a model call.

## Route-boundary repair

The exact configured author and referee command is now:

```text
hermes chat --provider openai-codex --query-file - --ignore-rules --quiet
```

Installed-Hermes inspection and a content-free smoke established that `--quiet` reserves stdout for
the final response and sends session diagnostics to stderr. The prior sealed route omitted that
flag, so CLI decoration could reach the producer's strict JSON or Markdown parser. The repair keeps
fixed argument vectors and strict parsing instead of stripping unknown decorations from model
output. Failed processes, process-start errors, and timeouts now expose only stable error categories;
raw external stdout, stderr, commands, paths, account labels, and prompt material do not enter the
public exception or systemd journal boundary.

The boundary applies ASVS 1.2.5, 2.2.1, 13.3.2, 14.2.3, 16.2.5, and 16.5.1: fixed documented
interfaces, explicit opt-in, bounded payloads, redacted diagnostics, and data-sharing consent remain
separate controls.

## Route-free F4 preparation

Historical calibration now uses schema v2 and records the bounded diagnostic procedure in its
sealed target contract. The current one-time procedure uses three repetitions, one adjacent-level
score-span tolerance, and the target-derived 0.25 separation threshold. These settings are
configurable evidence inputs, not permanent pipeline assertions. Every criterion result carries an
exact selected-post passage and a concise reason, and the parser verifies the passage directly.

Prompt capture and attestation advance to schemas v3 without compatibility paths. A passing
deterministic attestation reports `review_ready`, not activation readiness, and embeds the unchanged
central question plus an artifact-only review contract. The current procedure records two reviewers,
but the count is bounded and configurable. Each reviewer independently reads both complete selected
fixture posts and the sealed evidence, without a manager summary or other reviewer work. Every
required dimension cites an exact selected-post passage. F4 is accepted only when every submission
required by the sealed contract passes both fixtures.

Attestation now uses schema v4 and closes the repeated-sample provenance gap before live F4 work.
For the selected arm, it deterministically binds the first authority-ordered complete busy sample
and quiet sample without consulting a score or comparison outcome. Each reference names the exact
`selected.md` artifact and SHA-256. Later repetitions stay diagnostic. The route-free review loader
reopens those private files through descriptors and verifies their hashes, and reviewer submissions
must match those same post identities. This makes "both complete selected posts" exact without
turning repetition count, score values, or reviewer count into permanent prose-quality assertions.

The production-shaped route-free handoff exposed and closed one preproduction schema defect. The
fixture writer emitted the documented rich v2 identity, while the experiment loader still parsed a
retired reduced field shape used only by its synthetic test fixture. The writer and reader now use
one shared positive contract with no compatibility branch. The reader additionally proves packet,
projection, roster, mirror-summary, byte-count, and digest coherence.

After that repair, the existing approved artifacts loaded directly with their original sealed
identities; no fixture was recaptured or rotated. The busy fixture contains 34 evidence items and a
59,881-character projection. The quiet fixture contains 24 evidence items and a 59,593-character
projection. Route-free rendering accepted the complete four-arm matrix for both fixtures. All eight
production-shaped prompts stayed below the configured 72,000-character author limit, embedded the
exact sealed projection, and embedded any selected examples byte-for-byte. All six v4 renders kept
the unchanged complete-post question after ordinary whitespace normalization and excluded the
referee rubric. Configuration still records `external_model_data_sharing: false`; no Hermes process
or model route was invoked.

Post-repair verification passed 36 focused fixture, capture, contract-integration, and attestation
tests plus 858 Python and documentation hygiene checks. The complete Python 3.12.13 suite passed all
2,418 tests, and all eight retained E2E workflows passed. These durable tests exercise the shared
offline behavior and integrity boundary; they do not assert the live experiment's configured
repetition, threshold, or reviewer values.

## F4 through F6 acceptance

A sandboxed live-calibration attempt created private diagnostic artifact
`rubric-calibration-20260829t011223z-0aa0bca424`. Hermes could not initialize state and log files in
the restricted filesystem; all 15 records failed before a structured scorecard was available. That
artifact is incomplete diagnostic evidence, not a rubric result.

Configuration remains `external_model_data_sharing: false`; the mandatory F4 path used no model
egress. F4 accepted sealed capture
`8735621297882bdd9fdac08ccf7833849c2466cb11ee0e6e8fef1fe3e17048c1`, attestation
`prompt-experiment-attestation-6cfc303e23e8af22e53c50d5c01741600938e3d14ff14e880635cf9197d0aac3`,
and independent review evidence
`prompt-experiment-review-evidence-fa097dd766ef0a9e89b87a91bb1b64758a41a1367ba5db0e2234f028a4f49265`.
Both selected posts were independently accepted with passage grounding: busy SHA-256
`52d796...997d42` and quiet SHA-256 `bf23a22...9910`. Aug. 22-23 remain positive voice
references, Aug. 24-25 voice failures, and Aug. 26 the evidence/discovery failure.

F5 accepted the producer/publisher activation and bundle v5 boundary. The complete real page passed
through bundle import and a strict MkDocs build at
`blog/2026/08/26/letting-cancer-clicker-show-its-mutations/index.html`; 11 archived and public
assets were byte-identical. The 12-case crash-recovery evidence accepted the date-owned transaction.

F6 accepted a disposable-root staged run of `make_blog.py --yesterday`, the 04:00
America/Chicago schedule contract, missing import/build, coherent idempotent preflight, invalid
preservation, and DEVNULL behavior. Installed-unit state remains optional telemetry.

Permanent tests remain offline, deterministic behavior contracts. Capture, calibration, review,
crash matrix, staged build, and page inspection are one-time evidence; no permanent assertion fixes
their repetition counts, thresholds, reviewer count, internal key layout, or generated prose bytes.
F7 accepted the producer Python 3.12.13 suite (2,450 passed, 0 failed), publisher Python 3.13.5
suite (1,362 passed, 0 failed), publisher hygiene (310 passed), and a strict disposable MkDocs build.
The publication E2E verified the busy fixture/capture, exact quiet route without egress, active
activation, bundle v5, real importer, 11 assets, and
`blog/2026/08/26/letting-cancer-clicker-show-its-mutations/index.html`. The crash E2E accepted all
12 SIGKILL cases; the schedule E2E accepted the exact 04:00 America/Chicago
`make_blog.py --yesterday` DEVNULL path, missing-import/build, coherent repeat preflight, and invalid
preservation. Four independent requirements, security, test-policy, and maintainability audits
accepted after grounded fixes. Prompt hashes match approved values. The fixup is complete.
